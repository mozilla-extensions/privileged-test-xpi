{
  "name": "privileged",
  "version": "4.0",
  "scripts": {
    "build": "web-ext build -s extension/ --overwrite-dest --filename=privileged.xpi"
  },
  "dependencies": {
    "web-ext": "10.0.0"
  }
}
